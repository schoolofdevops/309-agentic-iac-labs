# Selected task context

The queue name must keep the default `course-jobs`. The requested change makes
the variable non-nullable. The accepted workflow may change only `main.tf` in an
isolated fixture copy. It may run format check, backend-disabled initialization,
and validation. It must stop before plan, apply, state, credentials, network, or
deployment.
